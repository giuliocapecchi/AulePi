[project]
name = "auleep-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.3",
    "pydantic>=2.0",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "vercel-blob>=0.3.0",
]

[tool.uv]
package = false
